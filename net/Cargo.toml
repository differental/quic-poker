[package]
name = "net"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"

[dependencies]
quinn = "0.11"
rustls = "0.23"
webpki-roots = "1"
rcgen = { version = "0.14", optional = true }
protocol = { path = "../protocol" }

[features]
dev = ["dep:rcgen"]
